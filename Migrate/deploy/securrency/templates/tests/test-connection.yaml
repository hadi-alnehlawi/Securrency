apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "securrency.fullname" . }}-test-connection"
  labels:
    {{- include "securrency.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "securrency.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
